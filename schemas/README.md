# Published schemas

Schemas are intentionally stable contracts. Core tables use ISO dates, deterministic string IDs, explicit quality/status enums, and provenance columns. New fields are additive; semantic changes require a new definition or release version.
